# Git
alias gs="git status"
alias ga="git add ."
alias gc="git commit -m"
alias gp="git push"
alias gl="git pull"

# Docker
alias dcu="docker compose up -d"
alias dcd="docker compose down"
alias dcl="docker compose logs -f"

# Dev
alias serve="npx serve"
alias nxdev="npx next dev"
alias pyd="python3 manage.py runserver"
